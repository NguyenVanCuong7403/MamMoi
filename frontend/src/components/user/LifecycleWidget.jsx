// Re-export from lifecycle folder for backwards compatibility
// This file maintains the original import path while using the refactored modular structure

export { default, normalizePhaseId, mapPhaseIdFromText } from "./lifecycle";
